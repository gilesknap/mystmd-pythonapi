FROM ghcr.io/diamondlightsource/ubuntu-devcontainer:resolute

# node + npm (Node 22), python and uv all come from the base image. node/npm are
# on PATH, so `npm ci` yields ./node_modules/.bin/myst (mystmd@1.10.1, pinned via
# package-lock.json). No language-specific setup is needed here.
